<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SalesOrderTracker.Views.WelcomePage"
             Title="Welcome"
             Background="{DynamicResource AppBackgroundBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackLayout Grid.Row="0" Spacing="6">
            <Label Text="Sales Order Tracker" Style="{StaticResource AppHeaderTitle}" />
            <Label Text="A modern, local-first order management demo" Style="{StaticResource AppHeaderSubtitle}" />
        </StackLayout>

        <Frame Grid.Row="1" Style="{StaticResource CardFrame}" Margin="0,18,0,0">
            <VerticalStackLayout Spacing="12">
                <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" />
                <Entry Placeholder="Password" IsPassword="True" Text="{Binding Password}" />

                <HorizontalStackLayout Spacing="8" VerticalOptions="Center">
                    <Label Text="Remember me" VerticalOptions="Center" />
                    <Switch IsToggled="{Binding RememberMe, Mode=TwoWay}" />
                </HorizontalStackLayout>

                <Button x:Name="LoginButton" Text="Login" Style="{StaticResource PrimaryButton}" />
            </VerticalStackLayout>
        </Frame>

        <Label Grid.Row="2" Text="Use any email and password to continue to the demo." HorizontalOptions="Center" VerticalOptions="End" FontSize="12" TextColor="{DynamicResource TextSecondary}" />
    </Grid>

</ContentPage>
